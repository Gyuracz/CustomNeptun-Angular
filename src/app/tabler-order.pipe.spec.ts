import { TablerOrderPipe } from './tabler-order.pipe';

describe('TablerOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TablerOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
